# =====================================================
# ApeAcademy — Environment Variables
# Copy to .env locally. Add ALL of these in Vercel:
# Project → Settings → Environment Variables
# =====================================================

# ── SUPABASE (Required) ───────────────────────────
# Get from: supabase.com → Your Project → Settings → API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here

# ── CLOUDINARY (Required for file uploads) ────────
# Get from: cloudinary.com → Dashboard
VITE_CLOUDINARY_CLOUD_NAME=your-cloud-name
# Create an UNSIGNED upload preset in:
# Cloudinary → Settings → Upload → Add upload preset → Mode: Unsigned
VITE_CLOUDINARY_UPLOAD_PRESET=your-unsigned-preset-name

# ── WISE PAYMENTS (Required for live payments) ────
# Get from: wise.com → Settings → API tokens
VITE_WISE_API_TOKEN=your-wise-api-token
# Get from: GET https://api.wise.com/v1/profiles (after auth)
VITE_WISE_PROFILE_ID=your-wise-profile-id
# true = sandbox/testing, false = live real payments
VITE_WISE_SANDBOX=true
